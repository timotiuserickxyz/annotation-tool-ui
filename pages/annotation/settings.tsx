import React from 'react';
import { Settings } from '../../src/components/page/Annotation/Settings';

const annotationSettings: React.FC = () => {
  return <Settings />;
};

export default annotationSettings;
