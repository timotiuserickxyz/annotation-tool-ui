import React from 'react';
import { ProjectSettings } from '../../src/components/page/Annotation/ProjectSettings';

const annotationProjectSettings: React.FC = () => {
  return <ProjectSettings />;
};

export default annotationProjectSettings;
